FROM maven:3.9.9-eclipse-temurin-17 AS builder
WORKDIR /app
COPY . .
RUN mvn clean package -DskipTests -pl embedding-worker -am

FROM eclipse-temurin:17-jre-jammy
WORKDIR /app
COPY --from=builder /app/embedding-worker/target/vectorsync-embedding-worker-*.jar app.jar
EXPOSE 8082
ENTRYPOINT ["java", "-jar", "app.jar"]

# Made with Bob
